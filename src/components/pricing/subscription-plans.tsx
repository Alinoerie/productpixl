/** @deprecated Monthly plans are rendered via PricingPlanCards — kept for imports/tests. */
export { PRICING_PLAN_COLUMNS as SUBSCRIPTION_PLANS } from "@/lib/pricing-plans";

export function SubscriptionPlans() {
  return null;
}
